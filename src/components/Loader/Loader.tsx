import { StyledLoader } from './styles';
import { ReactComponent as LoaderIcon } from 'src/assets/icons/loader.svg';

export const Loader = () => (
  <StyledLoader>
    <LoaderIcon />
  </StyledLoader>
);
